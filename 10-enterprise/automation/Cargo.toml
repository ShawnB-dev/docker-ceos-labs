[package]
name = "log-parser-app"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
tokio-stream = "0.1"
regex = "1"
chrono = { version = "0.4", features = ["serde"] }
anyhow = "1.0"
futures = "0.3"